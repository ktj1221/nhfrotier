# MockupGen 기능 명세서

**대상 화면:** `project.html` (프로젝트 상세 / 목업 작업 화면)
**서비스명:** MockupGen — AI 기반 화면 목업 · 기획서 자동 생성 도구

---

## 1. 문서 정보

| 항목 | 내용 |
|---|---|
| 문서명 | MockupGen 기능 명세서 |
| 버전 | v1.0 |
| 작성일 | 2026-08-30 |
| 분석 대상 | `https://ktj1221.github.io/claude_test/project.html?id=<projectId>` |
| 소스 규모 | 단일 HTML 파일 / 1,135 라인 (마크업 + 스타일 + 스크립트 통합) |
| 연관 화면 | `index.html` (대시보드 · 프로젝트 목록 · API 키 설정 · 시드 데이터 생성) |

---

## 2. 개요

### 2.1 목적

기획자가 작성한 **기획안 텍스트**를 입력하면 AI가 **실행 가능한 HTML 목업 화면**을 생성하고,
팀원이 버전별로 **의견 · 채팅**으로 피드백하며, 최종적으로 **한국어 화면 기획서(HTML)** 까지
자동 생성하는 기획–디자인 협업 워크플로우 도구.

### 2.2 범위

본 명세서는 `project.html` 한 화면이 제공하는 전 기능을 다룬다.
프로젝트 생성 · 삭제 · API 키 설정은 `index.html` 소관이므로 참조 수준으로만 기술한다.

### 2.3 용어 정의

> **ID 네임스페이스**: 본 문서의 `PFR-xx-yy`는 프로토타입 역공학 명세 ID로, 제품 정본 PRD의 `FR-01~12`([기능명세 인덱스](개발문서/기능명세/00_INDEX.md))와 무관하다.

| 용어 | 정의 |
|---|---|
| 프로젝트 | 하나의 화면 개선 과제 단위. 목업 · 참조화면 · 담당자 · 채팅을 소유 |
| 목업(Mockup) | AI가 생성한 자체 완결형 HTML 문서 1건. 버전 번호를 가짐 |
| 버전 | 동일 프로젝트 내 목업의 순번 (v1, v2, …). 생성 시 자동 증가 |
| 참조 화면 | AI에게 디자인 스타일을 학습시키기 위해 업로드하는 기존 화면 이미지 |
| 의견(Comment) | **목업 버전 단위**로 달리는 피드백 |
| 채팅(Chat) | **프로젝트 단위**로 이어지는 대화 |
| 현재 사용자 | 헤더에서 선택한 활동 주체. 로그인이 아닌 계정 전환 방식 |
| 기획서(Spec) | 목업 캡처 이미지 + 기획안을 근거로 AI가 작성한 7섹션 한국어 문서 |

---

## 3. 시스템 구성

### 3.1 기술 스택

| 계층 | 사용 기술 |
|---|---|
| 마크업 / 스타일 | HTML5, Tailwind CSS (CDN), 인라인 style |
| 스크립트 | Vanilla JavaScript (ES2020+, 빌드 도구 없음) |
| 데이터 저장 | 브라우저 localStorage (서버 · DB 없음) |
| AI 연동 | Anthropic Messages API 직접 호출 (`api.anthropic.com/v1/messages`) |
| 화면 캡처 | html2canvas 1.4.1 (cdnjs, 최초 사용 시 동적 로드) |
| 앱화 | PWA — `manifest.json`, `sw.js` Service Worker 등록 |

### 3.2 아키텍처 특징

- **백엔드 없음.** 모든 로직 · 데이터가 브라우저 안에서 완결된다.
- **단일 파일 배포.** 번들러 · 프레임워크 없이 정적 호스팅(GitHub Pages)만으로 동작.
- **AI 직접 호출.** 프록시 서버 없이 브라우저가 Anthropic API를 호출한다
  (`anthropic-dangerous-direct-browser-access: true` 헤더 사용).

### 3.3 외부 의존성

| 대상 | 용도 | 실패 시 영향 |
|---|---|---|
| `cdn.tailwindcss.com` | 전체 스타일 | 레이아웃 전면 붕괴 |
| `api.anthropic.com` | 목업 · 기획서 생성 | 생성 기능 불가 (조회는 정상) |
| `cdnjs.cloudflare.com` | html2canvas | 기획서 생성만 불가 |

---

## 4. 데이터 모델

### 4.1 localStorage 키 스키마

| 키 | 범위 | 값 |
|---|---|---|
| `mg_projects` | 전역 | `Project[]` |
| `mg_users` | 전역 | `User[]` |
| `mg_current_user` | 전역 | `User` (현재 활동 주체) |
| `mg_api_key` | 전역 | `string` (Anthropic API 키, **평문**) |
| `mg_refs_{projectId}` | 프로젝트 | `RefScreen[]` (최대 3) |
| `mg_mockups_{projectId}` | 프로젝트 | `Mockup[]` (최신순 정렬) |
| `mg_members_{projectId}` | 프로젝트 | `string[]` (userId 배열) |
| `mg_chat_{projectId}` | 프로젝트 | `ChatMessage[]` |
| `mg_comments_{mockupId}` | 목업 | `Comment[]` |

### 4.2 엔티티 정의

**Project**

| 필드 | 타입 | 설명 |
|---|---|---|
| `id` | string | UUID 또는 시드 ID (`seed-p1`) |
| `name` | string | 프로젝트명. 헤더 및 문서 타이틀에 표시 |
| `description` | string | 설명 |
| `createdAt` | ISO8601 | 생성 일시 |

**User**

| 필드 | 타입 | 설명 |
|---|---|---|
| `id` | string | UUID |
| `name` | string | 이름. 첫 글자가 아바타 문자로 사용됨 |
| `role` | string | 직책/부서. 미입력 시 `member` (UI 미표시) |
| `color` | string | 아바타 색상 키 (10색 팔레트에서 순차 배정) |

**RefScreen**

| 필드 | 타입 | 설명 |
|---|---|---|
| `id` | string | UUID |
| `name` | string | 화면 이름. 미입력 시 파일명 |
| `data` | string | **순수 base64** (data URL 접두사 제거됨) |
| `mimeType` | string | 예: `image/png` |
| `createdAt` | ISO8601 | 업로드 일시 |

**Mockup**

| 필드 | 타입 | 설명 |
|---|---|---|
| `id` | string | UUID |
| `version` | number | 버전 번호 (기존 최대값 + 1) |
| `proposalContent` | string | 생성에 사용된 기획안 원문 |
| `htmlContent` | string | AI가 생성한 완전한 HTML 문서 |
| `description` | string \| null | 버전 메모 |
| `createdAt` | ISO8601 | 생성 일시 |

**Comment** — `id`, `userId`, `content`, `createdAt` / 목업 버전에 종속
**ChatMessage** — `id`, `userId`, `content`, `createdAt` / 프로젝트에 종속

### 4.3 데이터 생명주기

- 목업 삭제 → 해당 `mg_comments_{mockupId}` 동시 삭제 (연쇄 정리 수행)
- 프로젝트 삭제(`index.html`) → 소속 목업의 댓글 + refs/mockups/members/chat 키 일괄 삭제
- 사용자 삭제 기능은 **없음**

---

## 5. 화면 구조

### 5.1 데스크톱 레이아웃 (≥768px)

```
┌──────────────────────────────────────────────────────────────┐
│ 헤더 (56px)   ← 뒤로 | 로고 | 프로젝트명 |      사용자 스위처  │
├───────────┬────────────────────────────────┬─────────────────┤
│ 좌 사이드바 │        중앙 목업 뷰어           │   우 패널       │
│   288px   │  ┌ 툴바 ──────────────────────┐ │    320px       │
│           │  │ v2 | 메모 | 뷰모드 · 기획서 │ │  ┌ 의견|채팅 ┐ │
│  참조     │  │      · HTML · 의견/채팅     │ │  │           │ │
│  생성     │  └────────────────────────────┘ │  │  타임라인  │ │
│  히스토리 │      <iframe srcdoc>            │  │           │ │
│  담당자   │                                 │  ├ 입력 폼 ──┤ │
└───────────┴────────────────────────────────┴─────────────────┘
```

### 5.2 모바일 레이아웃 (<768px)

- 좌/우 패널이 **하단 바텀시트**로 전환 (`translateY(100%)` → `0`)
- 하단 고정 5탭 네비게이션: 생성 / 히스토리 / 의견 / 채팅 / 담당자
- 반투명 오버레이 탭 또는 **아래로 스와이프(60px 이상)** 시 닫힘
- 뷰 모드 전환 버튼 · 우패널 토글 버튼 숨김
- `env(safe-area-inset-bottom)` 으로 iOS 홈 인디케이터 대응

---

## 6. 기능 명세

### 6.1 진입 및 초기화

| ID | 기능 | 상세 |
|---|---|---|
| PFR-01-01 | 프로젝트 식별 | URL 쿼리 `?id=` 로 프로젝트 조회 |
| PFR-01-02 | 유효성 검증 | `id` 누락 또는 프로젝트 미존재 시 `index.html`로 즉시 리다이렉트 |
| PFR-01-03 | 타이틀 반영 | 헤더 제목 및 문서 타이틀을 `{프로젝트명} - MockupGen` 으로 설정 |
| PFR-01-04 | 초기 렌더 | 참조화면 · 히스토리 · 담당자 · 채팅 · 의견 영역 일괄 렌더 |
| PFR-01-05 | 최신 버전 자동 선택 | 저장된 목업이 있으면 최신 버전 선택 후 히스토리 탭 전환 **(※ 9.1 이슈 — 현재 무효화됨)** |
| PFR-01-06 | 탭 뱃지 | 히스토리 / 담당자 탭 라벨에 건수 표시 (예: `히스토리(2)`) |
| PFR-01-07 | Service Worker | 페이지 로드 시 `./sw.js` 등록 (PWA 오프라인 지원) |

### 6.2 사용자 관리 (헤더)

| ID | 기능 | 상세 |
|---|---|---|
| PFR-02-01 | 사용자 스위처 | 헤더 우측 드롭다운. 전역 사용자 목록 표시, 현재 사용자에 체크 표시 |
| PFR-02-02 | 사용자 전환 | 선택 시 `mg_current_user` 갱신 → 헤더 아바타 · 의견/채팅 입력 폼 즉시 재구성 |
| PFR-02-03 | 사용자 생성 | 이름(필수) + 직책/부서(선택) 입력 → 생성 즉시 현재 사용자로 지정 |
| PFR-02-04 | 아바타 규칙 | 이름 첫 글자 + `COLORS[users.length % 10]` 자동 색상 배정 |
| PFR-02-05 | 외부 클릭 닫기 | 드롭다운 외 영역 클릭 시 자동 닫힘 |

> **비고:** 인증·인가 없음. 누구나 임의 사용자로 전환 가능한 데모용 계정 전환 모델.

### 6.3 참조 화면 관리 (참조 탭)

| ID | 기능 | 상세 |
|---|---|---|
| PFR-03-01 | 업로드 | 이미지 파일 선택 → base64 변환 후 저장. 화면 이름 선택 입력 |
| PFR-03-02 | 개수 제한 | **최대 3장.** 3장 도달 시 업로드 영역 자동 숨김, 라벨에 `(n/3)` 표시 |
| PFR-03-03 | 파일 검증 | MIME `image/*` 여부, 크기 5MB 이하 검사 |
| PFR-03-04 | 저장 실패 처리 | `QuotaExceededError` 감지 시 "저장 공간 부족" 전용 안내 |
| PFR-03-05 | 썸네일 그리드 | 3열 정사각 그리드, 하단 이름 오버레이, 호버 시 삭제 버튼 노출 |
| PFR-03-06 | 삭제 | 확인 대화상자 후 제거 |
| PFR-03-07 | 반영 힌트 | 생성 탭 하단에 `참조 화면 n개 스타일 반영` 안내 표시 |

**예외 처리**

| 상황 | 메시지 |
|---|---|
| 4장째 업로드 | 최대 3개까지 등록 가능합니다. |
| 비이미지 파일 | 이미지 파일만 업로드 가능합니다. |
| 5MB 초과 | 파일 크기는 5MB 이하여야 합니다. |
| 용량 부족 | 저장 공간이 부족합니다. 기존 참조 화면을 삭제 후 다시 시도하세요. |

### 6.4 목업 생성 (생성 탭)

| ID | 기능 | 상세 |
|---|---|---|
| PFR-04-01 | 입력 | 버전 메모(선택, 1줄) + 기획안 내용(**필수**, 14행, 예시 placeholder 제공) |
| PFR-04-02 | 사전 검증 | ① 기획안 공백 여부 ② API 키 설정 여부 |
| PFR-04-03 | 생성 중 UI | 중앙 영역을 스피너 화면으로 전환, 버튼 비활성화 + `생성 중...` |
| PFR-04-04 | 참조 반영 안내 | 참조화면 존재 시 `참조 화면 n개의 스타일을 분석 중입니다.` 표시 |
| PFR-04-05 | AI 호출 | 참조 이미지 + 기획안을 멀티모달 메시지로 전송 (7절 참조) |
| PFR-04-06 | 응답 후처리 | html 코드펜스 감지 시 내부 내용만 추출 |
| PFR-04-07 | 버전 채번 | `max(기존 version) + 1`. 중간 버전 삭제 후에도 번호 충돌 없음 |
| PFR-04-08 | 저장 및 표시 | 목록 선두에 저장 → 즉시 선택·렌더 → 입력 필드 초기화 |
| PFR-04-09 | 완료 알림 | 토스트 `v{n} 목업이 생성됐습니다` (2.5초 후 소멸) |
| PFR-04-10 | 실패 처리 | 빈 화면 복귀 + 생성 탭 재활성화 + `생성 실패: {메시지}` 인라인 표시 |

**예외 처리**

| 상황 | 메시지 |
|---|---|
| 기획안 미입력 | 기획안 내용을 입력해주세요. |
| API 키 미설정 | API 키가 설정되지 않았습니다. 대시보드에서 API 키를 먼저 설정해주세요. |
| API 오류 | 생성 실패: {API 오류 메시지 또는 HTTP 상태코드} |

### 6.5 목업 뷰어 (중앙 영역)

| ID | 기능 | 상세 |
|---|---|---|
| PFR-05-01 | 4단 상태 | `empty`(안내) / `generating`(스피너) / `mockup`(뷰어) / 툴바 표시 여부 연동 |
| PFR-05-02 | 렌더링 | `iframe srcdoc` 으로 HTML 문서 격리 렌더 |
| PFR-05-03 | 툴바 정보 | 버전 뱃지 `v{n}` + 버전 메모 표시 |
| PFR-05-04 | 뷰 모드 | **데스크톱**(최대 1280×800) ↔ **모바일**(390×844) 전환 |
| PFR-05-05 | HTML 다운로드 | Blob 생성 → `mockup-v{n}.html` 파일 저장 |
| PFR-05-06 | 우패널 토글 | 의견/채팅 패널 열기·닫기 (버튼 라벨·색상 연동, 데스크톱 전용) |
| PFR-05-07 | 빈 상태 안내 | 데스크톱/모바일 각각 다른 문구 표시 |

### 6.6 버전 히스토리 (히스토리 탭)

| ID | 기능 | 상세 |
|---|---|---|
| PFR-06-01 | 목록 | 최신순. 버전 번호 · 메모 · 기획안 요약(2줄 말줄임) · 생성 일시(한국 로케일) |
| PFR-06-02 | 선택 | 클릭 시 해당 버전 로드, 선택 항목에 좌측 인디고 보더 + 배경 강조 |
| PFR-06-03 | 삭제 | 확인 대화상자 → 목업 + 해당 버전 댓글 동시 삭제 |
| PFR-06-04 | 삭제 후 처리 | 삭제 대상이 현재 선택 버전이면 빈 화면으로 복귀 |
| PFR-06-05 | 빈 상태 | `아직 생성된 목업이 없습니다.` |

### 6.7 담당자 관리 (담당자 탭)

| ID | 기능 | 상세 |
|---|---|---|
| PFR-07-01 | 목록 | 아바타 · 이름 · 직책 표시 |
| PFR-07-02 | 추가 | 전역 사용자 중 **미등록자만** 셀렉트에 노출 → 추가 |
| PFR-07-03 | 제거 | 확인 대화상자 후 제거 |
| PFR-07-04 | 뱃지 동기화 | 추가·제거 시 탭 라벨 건수 즉시 갱신 |
| PFR-07-05 | 안내 | 새 사용자는 헤더 우상단에서 추가하도록 유도 문구 표시 |

### 6.8 의견 (우패널 · 의견 탭)

| ID | 기능 | 상세 |
|---|---|---|
| PFR-08-01 | 범위 | **목업 버전 단위.** 버전 전환 시 해당 버전의 의견만 표시 |
| PFR-08-02 | 작성 | 아바타 + 텍스트영역. **Enter 전송 / Shift+Enter 줄바꿈** |
| PFR-08-03 | 표시 | 작성자 · 시간(오늘이면 시각, 아니면 월/일 + 시각) · 줄바꿈 보존 |
| PFR-08-04 | 삭제 | **본인 작성 건에만** 삭제 버튼 노출 |
| PFR-08-05 | 자동 스크롤 | 작성 직후 목록 최하단으로 이동 |
| PFR-08-06 | 가드 | 목업 미선택 / 사용자 미선택 시 각각 안내 문구 표시 및 입력 차단 |

**빈 상태 문구**

| 상황 | 문구 |
|---|---|
| 목업 미선택 | 목업 버전을 선택하면 의견을 작성할 수 있습니다. |
| 의견 0건 | 아직 의견이 없습니다. 첫 번째 의견을 남겨보세요! |
| 사용자 미선택 | 의견을 남기려면 상단에서 사용자를 선택하세요. |

### 6.9 채팅 (우패널 · 채팅 탭)

| ID | 기능 | 상세 |
|---|---|---|
| PFR-09-01 | 범위 | **프로젝트 단위.** 버전과 무관하게 하나의 대화 스레드 |
| PFR-09-02 | 말풍선 UI | 본인 메시지는 우측 정렬 + 인디고 배경, 타인은 좌측 + 회색 배경 |
| PFR-09-03 | 발신자 표시 | 타인 메시지에만 이름 라벨 노출 |
| PFR-09-04 | 전송 | 폼 제출 (Enter 전송) |
| PFR-09-05 | 자동 스크롤 | 렌더 및 전송 시 최하단 유지 |
| PFR-09-06 | 가드 | 사용자 미선택 시 입력 폼 숨김 + 안내 문구 |

### 6.10 화면 기획서 생성 (툴바 · 기획서)

2단계 파이프라인으로 동작한다.

| ID | 기능 | 상세 |
|---|---|---|
| PFR-10-01 | 사전 검증 | 목업 선택 여부 · API 키 설정 여부 (미충족 시 토스트) |
| PFR-10-02 | 모달 오픈 | 전체화면 모달 + 진행 상태 메시지 표시 |
| PFR-10-03 | **1단계 캡처** | DOMParser로 목업 HTML 파싱 → style 추출 + body 내용을 화면 밖 390px 컨테이너에 재구성 → html2canvas로 PNG 캡처 → base64 반환 |
| PFR-10-04 | 진행 안내 | `목업 화면을 캡처하고 있습니다...` → `AI가 기획서를 작성하고 있습니다...` |
| PFR-10-05 | **2단계 작성** | 기획안 텍스트 + 캡처 이미지를 AI에 전달, 한국어 7섹션 기획서 HTML 요청 |
| PFR-10-06 | 이미지 삽입 | 응답 내 `__MOCKUP__` 플레이스홀더를 base64 이미지로 치환 |
| PFR-10-07 | 폴백 | 플레이스홀더 누락 시 body 종료 직전에 이미지 블록 자동 추가 |
| PFR-10-08 | 미리보기 | 샌드박스 iframe으로 결과 렌더, 부제에 `v{n} — {메모}` 표시 |
| PFR-10-09 | 다운로드 | `기획서-v{n}.html` 파일 저장 |
| PFR-10-10 | 실패 처리 | 모달 닫고 토스트 `기획서 생성 실패: {메시지}` |

**기획서 구성 (AI 지시 사항)**

| # | 섹션 |
|---|---|
| 1 | 문서 정보 (버전 · 작성일 · 작성자 테이블) |
| 2 | 화면 개요 |
| 3 | 기획 배경 및 목적 |
| 4 | 화면 구성 설명 (목업 스크린샷 분석 기반) |
| 5 | 주요 기능 정의 (번호 목록) |
| 6 | UX 고려사항 |
| 7 | 목업 화면 (캡처 이미지) |

**문서 스타일 규격:** 흰 배경 / 최대 폭 800px 중앙 정렬 / 패딩 48px / 강조색 `#4f46e5` / 인라인 CSS 전용 / 전문 한국어.

### 6.11 모바일 UI

| ID | 기능 | 상세 |
|---|---|---|
| PFR-11-01 | 하단 5탭 | 생성 / 히스토리 / 의견 / 채팅 / 담당자 |
| PFR-11-02 | 바텀시트 | 좌·우 패널이 하단에서 슬라이드 업 (최대 높이 72vh, 상단 라운드 20px) |
| PFR-11-03 | 상호 배타 | 좌 패널 열면 우 패널 자동 닫힘 (동시 노출 방지) |
| PFR-11-04 | 토글 | 활성 탭 재탭 시 닫힘 |
| PFR-11-05 | 닫기 수단 | 오버레이 탭 / 아래로 60px 이상 스와이프 |
| PFR-11-06 | 자동 닫기 | 목업 선택 시 패널 자동 닫힘 (결과 즉시 확인) |
| PFR-11-07 | 안전 영역 | `env(safe-area-inset-bottom)` 반영 |

---

## 7. 외부 API 연동 명세

### 7.1 공통

| 항목 | 값 |
|---|---|
| Endpoint | `POST https://api.anthropic.com/v1/messages` |
| Model | `claude-opus-4-8` (하드코딩) |
| max_tokens | `8096` |
| Headers | `x-api-key`, `anthropic-version: 2023-06-01`, `content-type: application/json`, `anthropic-dangerous-direct-browser-access: true` |
| 오류 처리 | 응답 실패 시 본문의 `error.message` 또는 HTTP 상태코드로 예외 발생 |
| 응답 파싱 | `data.content[0].text` → 코드펜스 제거 |

### 7.2 목업 생성 요청

**System Prompt 요지**

- 완전한 자체 완결형 HTML 문서만 출력 (DOCTYPE 포함)
- **인라인 CSS 전용** — 외부 스타일시트 및 JavaScript 금지
- 현실적이고 전문적인 UI, 한국어 자연스럽게 지원
- 깔끔한 모던 디자인 (여백 · 색상 · 타이포그래피)
- 현실적인 더미 데이터 포함
- 참조 이미지가 있으면 색상 · 레이아웃 · 타이포그래피 · 컴포넌트 스타일을 분석해 반영

**User Content 구성 순서**

1. (참조화면 존재 시) 안내 텍스트 → 이미지 블록 + `Reference: "{이름}"` 텍스트 × N
2. `Create an HTML mockup for: {기획안}` + 완전한 HTML 문서 생성 요청

### 7.3 기획서 생성 요청

**System Prompt 요지**

- 한국 IT 기업용 전문 화면 기획서 작성자 역할
- 전체 한국어 작성 / 완전한 standalone HTML / 인라인 CSS 전용
- 6.10에 명시된 7개 섹션 필수 포함
- `__MOCKUP__` 플레이스홀더가 포함된 이미지 태그를 **원문 그대로** 배치

**User Content 구성**

1. `기획안: {proposalContent}`
2. 이미지 블록 (목업 캡처 PNG, base64)
3. 작성 요청 + 플레이스홀더 준수 지시

---

## 8. 비기능 요구사항

### 8.1 성능

- 정적 파일 단일 요청 + CDN 2건. 초기 로드 경량.
- 목업 생성 응답 시간은 AI API에 종속 (통상 수십 초). 진행 상태 UI로 대기 인지 완화.
- 이미지·HTML 전량을 localStorage에 보관 → 데이터 증가 시 파싱 비용 선형 증가.

### 8.2 보안

- 사용자 입력은 전 구간 이스케이프 함수(`& < > "`) 적용 — XSS 기본 방어.
- 요소 ID는 `crypto.randomUUID()` 기반이라 인라인 핸들러 문자열 주입 위험 낮음.
- **위험 요소는 9.2 · 9.3절 참조.**

### 8.3 호환성

- 모던 브라우저 기준 (`crypto.randomUUID`, optional chaining, nullish coalescing 사용).
- `randomUUID` 미지원 환경 폴백 존재.
- 반응형 분기점: 768px.

### 8.4 PWA

- `manifest.json` 연결, 테마 컬러 `#4f46e5`
- iOS 홈 화면 앱 메타태그, 192px 아이콘
- Service Worker 등록으로 오프라인 셸 캐싱

---

## 9. 제약사항 및 알려진 이슈

### 9.1 [기능 결함 · P0] 초기 진입 시 최신 목업이 표시되지 않음

**현상 (실제 브라우저 확인 완료)**

저장된 목업이 있어도 첫 진입 시 중앙 영역이 **빈 화면("목업 미리보기")** 으로 표시되고,
툴바(기획서 · HTML · 뷰 모드)도 노출되지 않는다. 히스토리에서 한 번 클릭해야 정상 표시된다.

**부작용 2건**

1. `selectedMockup`은 최신 버전으로 설정된 상태라 **우측 의견 패널에는 해당 버전 댓글이 이미 렌더링**된다 → "목업 없음"과 "댓글 있음"이 동시에 나타나는 모순 상태.
2. 히스토리 탭이 활성인데도 **생성 폼과 히스토리 목록이 좌측에 동시 표시**된다.

**원인**

스크립트 최하단 초기화 블록이 상단에서 수행한 자동 선택 결과를 무조건 덮어쓴다.

```js
// 상단 — 최신 목업 자동 선택
if (_initMockups.length > 0) { selectMockup(_initMockups[0]); switchLeftTab('history'); }

// 하단(1046행 부근) — 위 결과를 무효화
showView('empty');
document.getElementById('panel-generate').classList.remove('hidden');
```

**해결 방안**

하단 초기화 2줄을 자동 선택 블록 **앞으로** 이동하거나, `if (!selectedMockup)` 가드를 추가한다.

### 9.2 [보안 · P0] iframe 샌드박스 무력화

목업 뷰어가 `sandbox="allow-scripts allow-same-origin"` 조합을 사용한다.
이 두 값을 동시에 지정하면 샌드박스가 사실상 해제되어, iframe 내부 스크립트가
부모 페이지의 localStorage에 접근할 수 있다 — 즉 **`mg_api_key`(평문 API 키) 탈취가 가능**하다.

- 시스템 프롬프트에서 "JavaScript 금지"를 지시하지만 AI 출력은 보장되지 않으며,
  조작된 localStorage 데이터가 유입되는 경로도 존재한다.
- **해결:** `allow-same-origin` 제거. (기획서 모달의 iframe은 `allow-scripts`만 사용해 안전)

### 9.3 [보안 · P1] API 키 평문 저장 및 브라우저 직접 전송

`mg_api_key`가 localStorage에 암호화 없이 저장되고, 브라우저에서 Anthropic으로 직접 전송된다.
공용 PC 사용, 확장 프로그램, XSS 어느 경로로든 키가 그대로 노출된다.
운영 전환 시 **서버 프록시 경유 방식**으로 변경 필요.

### 9.4 [기능 · P1] 협업 데이터가 실제로 공유되지 않음

담당자 · 의견 · 채팅 등 협업 UI를 갖췄으나 저장소가 브라우저 로컬이므로
**다른 사람 · 다른 기기에서는 전혀 보이지 않는다.** 현재 구조는 단일 사용자 데모에 한정된다.

### 9.5 [기능 · P2] 피드백 루프 단절

의견에 "버튼을 44px로 키워주세요"라고 작성해도 그 내용이 AI 재생성에 전달되지 않는다.
다음 버전을 만들려면 기획안을 처음부터 다시 작성해야 하며,
**이전 버전 기반 수정 · 버전 간 비교(diff) 기능이 없다.**

### 9.6 [기술 · P3] 저장 용량 한계

참조 이미지 base64 + 목업 HTML 전량이 localStorage(통상 5–10MB)에 적재된다.
이미지 수 장이면 한도에 도달하며, 현재 Quota 예외 처리는 **참조 화면 업로드 경로에만** 존재한다.
목업 저장 경로에는 방어 코드가 없다.

### 9.7 [기술 · P3] 기획서 캡처 정합성

캡처 로직은 iframe 자체가 아니라 HTML을 **오프스크린 div에 재렌더**한 뒤 촬영한다.
head 내 일부 스타일, 외부 리소스, `position: fixed` 요소 등은 실제 미리보기와 다르게 잡힐 수 있다.

### 9.8 기타

| 항목 | 내용 |
|---|---|
| 모델 하드코딩 | `claude-opus-4-8`이 코드에 직접 기입 — 모델 교체 시 소스 수정 필요 |
| 참조 무결성 | 사용자 삭제 기능이 없으나, 데이터 직접 조작 시 기존 댓글 작성자가 `?`로 표시됨 |
| 인증 부재 | 사용자 전환에 검증 없음 — 누구나 타인 명의로 작성 가능 |
| 동시성 | 다중 탭에서 동일 프로젝트 편집 시 `storage` 이벤트 미처리로 마지막 쓰기가 승리 |

---

## 10. 개선 제안 (Backlog)

| 우선순위 | 항목 | 기대 효과 |
|---|---|---|
| P0 | 9.1 초기 렌더 순서 수정 | 첫 진입 UX 정상화 (2줄 이동으로 해결) |
| P0 | `allow-same-origin` 제거 | API 키 탈취 경로 차단 |
| P1 | 서버 프록시 경유 AI 호출 | API 키 비노출, 사용량 통제 |
| P1 | 백엔드 + 실시간 동기화 도입 | 실질적 팀 협업 기능 확보 |
| P2 | 의견 → 재생성 반영 (다음 버전 프롬프트 자동 구성) | 피드백 루프 완성, 반복 작업 제거 |
| P2 | 버전 간 비교(diff) 뷰 | 변경점 리뷰 효율 향상 |
| P3 | IndexedDB 전환 + 이미지 압축 | 용량 한계 해소 |
| P3 | 모델 · max_tokens 설정 UI 분리 | 운영 유연성 |
| P3 | 목업 저장 시 Quota 예외 처리 | 데이터 유실 방지 |

---

## 11. 데모 시드 데이터 (참고)

`index.html`의 `seedDemoData()`가 최초 방문 시 자동 생성한다.

| 구분 | 내용 |
|---|---|
| 사용자 | 김태정(PM) · 이수진(UI 디자이너) · 박준호(개발팀) |
| 프로젝트 1 | `seed-p1` — 농협 올원뱅크 앱 리뉴얼 (목업 v1·v2, 의견 5건, 채팅 6건) |
| 프로젝트 2 | `seed-p2` — SmartRoom 회의실 예약 앱 (목업 v1, 의견 2건, 채팅 3건) |
| 특징 | v1 → v2 개선 서사(여백 확대, 터치 영역 44px 확보, 지출 분석 위젯 추가)가 의견·채팅에 반영되어 있음 |
